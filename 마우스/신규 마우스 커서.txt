void OnUpDate(number delta){
마우스의 위치를 실시간으로 받아 신규 마우스 커서의 이미지를 동기화 시킨다.

	local mouse_position = _InputService:GetCursorPosition()
	_InputService:SetCursorVisible(true)					 -- 기본 커서이미지 활성화
	self.Entity.TransformComponent.WorldPosition.x = mouse_position.x
	self.Entity.TransformComponent.WorldPosition.y = mouse_position.y
	self.Entity.TransformComponent.WorldPosition.z = 0

마우스의 위치가 버튼 위에 있지 않을 시 기본 신규 커서 이미지로 설정
	local bool = _InputService:IsPointerOverUI()
	if bool == false then
		self.Entity.SpriteGUIRendererComponent.ImageRUID = "7913dc217ce54e3697e0a430916af5d2"
	end
}