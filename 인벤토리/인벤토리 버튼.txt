ButtonClickEvent(ButtonClickEvent event){
UI의 인벤토리 버튼 클릭시 인벤토리 할성화
	local Entity = event.Entity
	if self.inventory.Enable then
		self.inventory.Enable = false
	else
		self.inventory.Enable = true
	end
}